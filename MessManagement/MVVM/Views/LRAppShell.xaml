<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MessManagement.MVVM.Views.LRAppShell"
             xmlns:views="clr-namespace:MessManagement.MVVM.Views"
             Title="LRAppShell"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             Shell.FlyoutBehavior="Disabled"
             Shell.BackgroundColor="{AppThemeBinding Light='#F7F8FA',Dark=black}"
             Shell.TitleColor="{AppThemeBinding Light=Black,Dark=White}"
             Shell.ForegroundColor="{AppThemeBinding Light=Black,Dark=White}">
    <Shell.Resources>
        <ResourceDictionary>
            <!-- Apply StatusBarBehavior to all ContentPages -->
            <Style TargetType="ContentPage">
                <Setter Property="Behaviors">
                    <Setter.Value>
                        <toolkit:StatusBarBehavior
                        StatusBarColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                        StatusBarStyle="{AppThemeBinding Light=LightContent, Dark=LightContent}" />
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Shell.Resources>
    <ShellContent Route="LoginPage"
                  ContentTemplate="{DataTemplate views:LoginPage}" />
</Shell>